// Author: CastorYe
// Time: 2023-09-08 16:53:14
#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef long double ld;

void solve() {
    int n, q; cin >> n;
    vector<int> ls(n);
    for (auto &x : ls) cin >> x;
    string s; cin >> s >> q;

    while (q -- ) {
        int op; cin >> op;

        if (op == 1) {
            int l, r; cin >> l >> r;

            
        }
    }
}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    int T; cin >> T;
    while (T -- ) solve();
    return 0;
}
