#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef long double ld;

const int N = 110;
int ls_insert[N], ls_merge[N];

void solve() {
    int n; cin >> n;
    for (int i = 0; i < n; i ++) {
        int k; cin >> k;
        ls_insert[i] = k, ls_merge[i] = k;
    }

    
}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    solve();
    return 0;
}