#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef long double ld;

void solve() {
    int n, k; cin >> n >> k;
    queue<int> que;
    vector<int> ls;

    for (int i = 0; i < n; i ++) {
        int k; cin >> k; ls.push_back(k);
    }

    
}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    solve();
    return 0;
}